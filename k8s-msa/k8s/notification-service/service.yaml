# ClusterIP: order-service 내부에서만 호출
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: msa
  labels:
    app: notification-service
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - port: 8000
      targetPort: 8000

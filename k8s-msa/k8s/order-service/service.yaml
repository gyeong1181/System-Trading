# NodePort: 주문 API 진입점 (외부 테스트용)
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: msa
  labels:
    app: order-service
spec:
  type: NodePort
  selector:
    app: order-service
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30803

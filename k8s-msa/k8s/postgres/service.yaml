# ClusterIP: 서비스들이 내부에서만 DB 접근
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: msa
  labels:
    app: postgres
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432

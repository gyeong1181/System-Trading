# ClusterIP: order-service 내부 DNS로만 호출
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: msa
  labels:
    app: product-service
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
    - port: 8000
      targetPort: 8000

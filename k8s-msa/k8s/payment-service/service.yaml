# ClusterIP: order-service 내부 DNS로만 호출. 결제 API 외부 직접 노출 금지.
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: msa
  labels:
    app: payment-service
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - port: 8000
      targetPort: 8000

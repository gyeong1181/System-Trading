# ClusterIP: Redis는 product-service만 접근. 외부 노출 불필요.
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: msa
  labels:
    app: redis
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
